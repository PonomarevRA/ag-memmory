interface:
  display_name: "Agent Memory"
  short_description: "Maintain compact project knowledge"
  default_prompt: "Use $agent-memory to retrieve and capture compact, reusable project knowledge."

policy:
  allow_implicit_invocation: true
