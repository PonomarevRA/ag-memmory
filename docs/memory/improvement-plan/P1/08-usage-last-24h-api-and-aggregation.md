# P1.8. API использования за последние 24 часа

**Владелец:** Core + Storage
**Зависимости:** P1.5, P1.6, P1.7
**Статус:** planned

## Цель

Отдать безопасную агрегированную и постраничную историю уже записанных событий.

## Сделать

- Агрегировать окно 24 часа с явно указанной UTC/local границей и pagination для событий.
- Возвращать tokens delivered estimate, potential savings только при принятом P0.3 baseline и разрешённый query descriptor.
- Помечать период до включения telemetry как отсутствие данных, а не нулевое использование.

## Приёмка

API не фабрикует исторические значения, redacts поля по политике и правильно считает край окна.
