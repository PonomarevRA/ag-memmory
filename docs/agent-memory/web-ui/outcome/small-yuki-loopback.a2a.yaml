schema: agent-memory-object/v1
id: web-ui.outcome.small-yuki-loopback
kind: outcome
area: web-ui
status: completed
title: Connected the local Small Yuki llama-server to the Web chat gateway
summary: The Web host can call Small Yuki through its loopback OpenAI-compatible chat-completions endpoint without exposing a provider key to the browser.
recorded: "2026-08-08"
updated: "2026-08-08"
tags: [web-ui, chat, small-yuki, llama-server, local-model]
sources:
  - kind: code
    ref: src/AgMemory.Web/Gateway/ModelGatewayOptions.cs
  - kind: code
    ref: src/AgMemory.Web/Gateway/OpenAiCompatibleChatGateway.cs
  - kind: local-project
    ref: /Users/romanponomarev/Documents/Codex/2026-08-08/rfr/work/small-yuki/scripts/start-llama.sh
payload:
  change: Added the server-only ModelGateway DisableThinking option and configured the running Web host for Small Yuki's loopback OpenAI-compatible endpoint.
  verification: A protected /api/chat request completed with a non-demo text response from model alias small-yuki-local.
  result: Small Yuki requires chat_template_kwargs.enable_thinking=false for concise visible content; reasoning_content is intentionally neither read nor rendered.
  consequence: Keep DisableThinking opt-in because it is a llama-server/Qwen request extension, not a universal OpenAI-compatible API field.
